<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="0dp"
        android:color="@color/accent" />
    <corners
        android:bottomLeftRadius="@dimen/bg_dialog_button_radius"
        android:bottomRightRadius="@dimen/bg_dialog_button_radius"
        android:topLeftRadius="@dimen/bg_dialog_button_radius"
        android:topRightRadius="@dimen/bg_dialog_button_radius" />

    <solid android:color="@color/bg_color_accent"/>

</shape>